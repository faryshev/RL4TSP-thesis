{
  "seed": 42,
  "output_root": "results/full",
  "scale": 10.0,
  "device": "cpu",
  "n_train": 20,
  "batch_size": 64,
  "epochs": 3000,
  "learning_rate": 0.0001,
  "hidden_dim": 128,
  "models": ["attention", "pointer"],
  "baselines": ["batch_greedy_mean", "greedy_rollout"],
  "progress": true
}
